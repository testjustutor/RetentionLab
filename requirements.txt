# Python dependencies used by this project
#
# Install with:  python -m pip install -r requirements.txt
# NOTE: ffmpeg must also be installed on PATH separately.
# On Windows, install CPU PyTorch wheels directly if needed:
#     python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# NOTE: `webrtcvad` (a Resemblyzer dependency) builds a C++ extension that
# requires "Microsoft C++ Build Tools". If it fails to install, apply the
# lightweight `webrtcvad.py` stub that lives in the Python site-packages, or
# install Resemblyzer with:  python -m pip install --no-deps resemblyzer

# ---- Core / transcription -------------------------------------------------
openai-whisper
openai
whisperx
faster-whisper
torch
torchaudio
transformers
accelerate
huggingface_hub
onnxruntime

# ---- Speaker diarization --------------------------------------------------
resemblyzer
scikit-learn
soundfile
librosa
pyannote.audio
requests

# ---- Media / audio conversion ----------------------------------------------
moviepy
ffmpeg-python
noisereduce

# ---- AI SDKs / misc ---------------------------------------------------------
sentence-transformers
google-genai
deepgram-sdk

# ---- Database ---------------------------------------------------------------
mysql-connector-python
python-dotenv
