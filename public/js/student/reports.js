/**
 * public/js/student/reports.js
 *
 * Placeholder reports page for the student role. There is no dedicated
 * student-reports API yet, so this simply keeps the empty state visible.
 * Once a student-facing reports endpoint exists (e.g. GET /api/student/reports),
 * wire it up here the same way public/js/reviewer/*.js fetch their data.
 */

document.addEventListener('DOMContentLoaded', () => {
  // Nothing to fetch yet — the empty state in reports.html covers this.
});
